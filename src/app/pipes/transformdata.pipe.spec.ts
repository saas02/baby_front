import { TransformdataPipe } from './transformdata.pipe';

describe('TransformdataPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformdataPipe();
    expect(pipe).toBeTruthy();
  });
});
